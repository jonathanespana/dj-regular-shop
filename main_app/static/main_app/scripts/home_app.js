jQuery(function ($) {
    $('.product-slick').slick({
        dots: true,
        slidesToShow: 2,
        slidesToScroll: 1,
    });
    // using $ here will be safely even jQuery.noConflict() will be enabled
    
});